%h1 Products

- @products.each do |product|
    %div{id: dom_id(product)}
        = product.name
        = number_to_currency(product.price, unit: 'kr', format: '%n %u')
        = add_to_cart(product, 1, "Add to order") if user_signed_in?

